# Integrations

No external integrations are configured yet.

Future adapters must keep credentials outside this repository, record source provenance, expose read and write operations separately, support dry runs where possible, and require a recorded human approval before consequential writes. Integrations must not bypass workflow evaluation or governance records.

