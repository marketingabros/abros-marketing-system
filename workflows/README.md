# Workflows

Workflows define predictable sequences, state transitions, validation, and approval gates. A workflow may call a skill or a short-lived task agent for a bounded judgment step, but orchestration remains explicit and inspectable.

