# Evaluations

Evaluation is independent from generation. Generators do not mark their own work as approved. The initial quality gates are deliberately small and may be run by a human, a separate evaluator, or deterministic checks depending on the criterion.

